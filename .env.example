PORT=3001
NODE_ENV=development

# Leave DATABASE_URL blank locally — the backend defaults to SQLite (no install needed).
# On Render, this is injected automatically from the Blueprint database resource.
# DATABASE_URL=
