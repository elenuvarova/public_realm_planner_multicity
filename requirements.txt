# Pinned to the versions resolved in the repo .venv (the tested set).
# `~=` allows patch-level updates only (e.g. ~=1.1.3 → >=1.1.3,<1.2.0) for a
# reproducible-yet-patchable build; osmnx is bounded to its tested MAJOR because
# its API changed substantially across majors (1.x → 2.x).

# ── geospatial core ───────────────────────────────────────────────────────────
geopandas~=1.1.3
shapely~=2.1.2
pyproj~=3.7.2
pyogrio~=0.12.1        # geopandas vector I/O backend (read/write GPKG, SHP, GeoJSON)

# ── data / numerics ───────────────────────────────────────────────────────────
pandas~=3.0.3
numpy~=2.4.6
scipy~=1.17.1
scikit-learn~=1.8.0

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests~=2.34.2
openpyxl~=3.1.5        # ONS IMD 2019 .xlsx parsing

# ── OSM + network ─────────────────────────────────────────────────────────────
osmnx>=2.1,<3          # bound to tested MAJOR (2.x); API differs across majors
networkx~=3.6.1

# ── spatial indexing / hex grid ───────────────────────────────────────────────
h3~=4.5.0

# ── optimisation ──────────────────────────────────────────────────────────────
pulp~=3.3.2            # MCLP exact ILP solver (CBC backend)

# ── tests ─────────────────────────────────────────────────────────────────────
pytest~=8.3            # engine invariant suite (tests/)

# rasterstats>=0.19     # for GHSL population raster (Phase 2)
# tobler>=0.11          # for areal interpolation (Phase 2)
# pandana>=0.7          # for network accessibility (Phase 2)
# access>=1.1           # for 2SFCA (Phase 2)
# spopt>=0.6            # for MCLP via PySAL (senior path)
