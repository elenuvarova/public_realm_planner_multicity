databases:
  - name: public-realm-planner-db
    plan: free
    databaseName: app
    user: app

services:
  - type: web
    name: public-realm-planner-web
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: public-realm-planner-db
          property: connectionString
